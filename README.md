# Shorten URL Service

a simple url shorten service

## Setup

make **.env** file and add the following enviroment variabels:

```javascript
HOST=
```

## How to use

clone repo then:

```javacript
npm install
npm run build
npm start
```
